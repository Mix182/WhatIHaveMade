from turtle import Turtle as pn
#from pynput.keyboard import Listener, KeyCode

pen = pn()
"""
pen.pendown()
pen.pensize(5)

pen.right(90)

pen.left(90)

pen.forward(50)

pen.back(50)

pen.penup()

pen.pendown()

pen.clear()

def on_press(key):
    if key == KeyCode(char = "f"):
        if click_thread.running:
            click_thread.stop_clicking()
            
        else:
            click_thread.start_clicking()
            
    elif key == exit_key:
        click_thread.exit()
        listener.stop()

with Listener(on_press=on_press) as listener:
    listener.join()"""